package com.snakegame;

/**
 * Enum representing possible movement directions for the Snake.
 * 
 * Used by:
 * - Snake: to determine the current direction of movement.
 * - GamePanel: to update the direction based on key input.
 */
public enum Direction {
    UP, DOWN, LEFT, RIGHT
}
